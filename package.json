{
  "name": "e-commerce-store",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "build": "pnpm --dir client exec vite build",
    "build:client": "pnpm --dir client exec vite build",
    "dev:client": "pnpm --dir client dev",
    "dev:server": "pnpm --dir server dev"
  }
}
