/**
 * Payment Processor Interface
 * All payment processors must implement this interface
 */

// Imports removed as they are no longer used

// Interface is kept for structure, but currently empty as we access concrete implementation for checkout sessions
export interface IPaymentProcessor {
  // Add common methods here if needed in future
}










